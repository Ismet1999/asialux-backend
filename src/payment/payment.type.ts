export enum PaymentType {
  // cash, card, transfer
  CASH = 'cash',
  CARD = 'card',
  TRANSFER = 'transfer',
}

export enum PaymentStatus {
  // pending, success, rejected
  PENDING = 'pending',
  SUCCESS = 'success',
  REJECTED = 'rejected',
}
